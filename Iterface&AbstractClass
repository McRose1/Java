# 接口和抽象类的区别

## 接口的优点
一个类可以实现多个接口，接口可以让这个类不仅具有主类型的行为，而且具有其他的次要行为，比如 HashMap 主要类型是 Map，而 Cloneable 接口使它具有一个次要类型，这个类型说明它可以安全的克隆

## 接口的缺点
如果向一个 Java 接口加入一个新的方法时，所有实现这个接口的类都得编写具体的实现。

## 抽象类的优点
抽象类可以为部分方法（非抽象方法）提供默认的实现从而避免子类中重复代码，向抽象类中加入非抽象方法不会影响子类

## 抽象类的缺点
只能单继承

## 接口与抽象类的区别
1. 抽象类可以包含部分方法的实现，这是抽象类优于接口的一个主要地方
2. 由于 Java 的单继承，每个类这能从一个抽象类继承，但是每个类可以实现多个接口，使用接口还可以实现混合类型的类。接口可以继承多个接口，即接口间可以多重继承
3. 
